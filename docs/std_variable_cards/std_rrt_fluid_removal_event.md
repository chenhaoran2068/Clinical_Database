# Public Variable Card: std_rrt_fluid_removal_event

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_rrt_fluid_removal_event`
- standardized Chinese name: `RRT体外液体移除事件桥`
- standardized English name: `RRT fluid removal bridge event`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `intake_output_balance`
- standard unit: `mL`
- value type: current approved database assets still need explicit public normalization
- grain: current approved database assets still need explicit public normalization

## Cross-Database Standard Definition

Cross-database standardized RRT fluid removal bridge event with one shared variable identity across the approved database implementations listed below. Standard unit: mL.

## Shared Current Contract

- current reviewed-approved public display rule across listed databases: `not yet separately codified`
- current reviewed-approved cleaned-value range note is not yet identical across all listed databases
- current public grain rule: approved database implementations do not yet expose one identical grain; respect the published per-database grain (`event-level measurement`, `time-stamped treatment/device/IO event`) and do not silently collapse them into one generic interpretation
- current public interpretation rule: database-specific timing and anchoring semantics still require each database's approved local Layer 5 evidence package

## Approved Database Implementations

| database_id | publication name | value type | grain | default anchor family | primary retained value column | display rule | range note | latest_review_date |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AmsterdamUMCdb-1.0.2 | Renal replacement therapy fluid removal event | numeric_event_with_metadata | time-stamped treatment/device/IO event | ICU admission anchor | - | not yet separately codified | - | - |
| MIMIC-IV-3.1 | RRT fluid removal bridge event | numeric_with_metadata | event-level measurement | ICU admission anchor | std_rrt_fluid_removal_event | not yet separately codified | - | 2026-04-15 00:00:00 |

## Database-Specific Notes

### AmsterdamUMCdb-1.0.2

- no database-specific public caution is currently published for this database implementation

### MIMIC-IV-3.1

- `high` / `std_rrt_fluid_removal_event caution 1`: This bridge is an extracorporeal-removal augmentation layer, not a replacement for the approved measured charted ICU balance foundation.
- `medium` / `std_rrt_fluid_removal_event caution 2`: `machine_hourly_patient_removal_volume` is suitable as an extracorporeal-removal bridge component but still does not, by itself, equal whole-body cumulative fluid balance.
- `medium` / `std_rrt_fluid_removal_event caution 3`: `hemodialysis_output_volume` is often charted as a single end-of-session removal total for an intermittent dialysis course rather than a physiologically smooth hour-by-hour removal stream.
- `medium` / `std_rrt_fluid_removal_event caution 4`: Source `charttime` for removal rows may represent retrospective charting of prior removal, so downstream hour-level smoothing or episode-level redistribution remains a separate modeling choice rather than an opening bridge rule.
- `medium` / `std_rrt_fluid_removal_event caution 5`: `ultrafiltrate_output_volume_context` and the peritoneal-dialysis volume rows are retained as context-only evidence in opening v1 and must not be silently promoted into the default bridge numerator.

## Cross-Database Status

- Currently approved in: MIMIC-IV-3.1, AmsterdamUMCdb-1.0.2.
- current publication basis: cross-database public card synthesized from all reviewed-approved database assets listed below
- metadata source rule: shared public fields are emitted only when current approved database assets agree after light public normalization

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| AmsterdamUMCdb-1.0.2 | reviewed_approved | v1 | - |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-15 00:00:00 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
