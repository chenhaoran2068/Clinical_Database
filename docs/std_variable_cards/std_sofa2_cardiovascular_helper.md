# Public Variable Card: std_sofa2_cardiovascular_helper

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-04-22 09:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_sofa2_cardiovascular_helper`
- standardized Chinese name: `SOFA-2心血管辅助表`
- standardized English name: `SOFA-2 cardiovascular helper`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `scores`
- standard unit: none
- value type: `integer_score_with_rule_trace`
- grain: `event-level measurement`

## Standard Definition

Hourly ICU SOFA-2 cardiovascular helper line built on the retained std_sofa backbone, with explicit course-level vasoactive eligibility, true NE/Epi concurrency, dopamine-only special bins, and atomic mechanical circulatory support overrides.

## Default Presentation

- default display rule: `string`
- primary retained value column: `score_time_precision`
- default anchor type: `ICU admission anchor`
- current approved value range note: `datetime`

## Global Warnings

- `high` / `published_fallback_not_implemented`: The published MAP-only fallback for cardiovascular scores 2-4 is intentionally not implemented in opening v1. Preferred handling: Treat current helper as the strict source-faithful path that requires observed qualifying support evidence for scores >= 2.
- `medium` / `dopamine_special_scope`: The dopamine special path is applied only when dopamine is the sole qualifying non-NE/Epi agent set in the hour. Preferred handling: Use score_rule_branch_class and other_agent_set_overlapping_hour when performing sensitivity analyses.
- `medium` / `backbone_choice`: This helper reuses the retained std_sofa row universe rather than regenerating a helper-only full ICU grid. Preferred handling: Interpret coverage in the context of the chosen repository backbone and not as a standalone ICU-universe asset.
- `medium` / `nonpositive_rate_rows`: Hours can carry source nonpositive-rate caution evidence even though those rows do not drive score assignment. Preferred handling: Default analyses may keep these rows but stricter audits can exclude or review them separately.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-11 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
