# Public Variable Card: std_weight_admission_baseline

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-04-22 09:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_weight_admission_baseline`
- standardized Chinese name: `入院基线体重`
- standardized English name: `admission baseline weight`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `anthropometrics`
- standard unit: `kg`
- value type: `numeric measurement`
- grain: `encounter`

## Standard Definition

Encounter-level admission baseline weight for MIMIC derived from cleaned std_weight_event rows inside the approved admission-anchor window.

## Default Presentation

- default display rule: `round(2)`
- primary retained value column: `std_weight_kg_baseline`
- default anchor type: `hospital_admission_start`

## Global Warnings

- `high` / `Admission baseline is not a generic patient-level weight`: This asset represents encounter-start status, not a patient-level stable attribute. Preferred handling: If the downstream analysis is subject-level, define a separate approved subject-level aggregation instead of reusing this asset directly.
- `medium` / `Current MIMIC admission baseline coverage is limited by direct linked source availability`: The current build relies on direct hadm-linked ICU-side measurements from std_weight_event. Preferred handling: Treat current missingness partly as source-availability limitation rather than pure clinical absence.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-03-22 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
