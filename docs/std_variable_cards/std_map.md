# Public Variable Card: std_map

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-04-22 09:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_map`
- standardized Chinese name: `平均动脉压事件`
- standardized English name: `Mean arterial pressure event`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `vital_signs`
- standard unit: `mmHg`
- value type: `numeric measurement`
- grain: `event-level measurement`

## Standard Definition

Event-level standardized ICU mean arterial pressure asset for MIMIC retaining invasive, noninvasive, and combined representations while preferring direct MAP and supplementing with same-time SBP/DBP-derived MAP only when direct MAP is absent.

## Default Presentation

- default display rule: `integer`
- primary retained value column: `std_map_mmhg_cleaned`
- default anchor type: `ICU admission anchor`
- current approved value range note: `[0, 300] mmHg`

## Global Warnings

- `high` / `This is an event-level blood-pressure asset`: std_map retains one row per event-time representation, not one row per stay or admission. Preferred handling: If a baseline blood-pressure variable is needed later, derive a separate approved baseline asset.
- `high` / `Combined does not mean averaged`: Combined rows simply choose invasive first and noninvasive second at the same time; they do not average modalities. Preferred handling: Use bp_representation plus selected_modality to separate direct modality rows from convenience combined rows.
- `medium` / `MAP can be direct or derived`: Direct MAP is preferred; same-time SBP/DBP-derived MAP fills only when direct MAP is missing. Preferred handling: Use measurement_origin to separate direct MAP from derived MAP when needed.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-03-23 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
