# Public Variable Card: std_icu_rrt_fluid_removal_bridge_hourly

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_icu_rrt_fluid_removal_bridge_hourly`
- standardized Chinese name: `ICU RRT体外液体移除逐小时桥表`
- standardized English name: `ICU RRT fluid removal bridge hourly`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `intake_output_balance`
- standard unit: none
- value type: `mixed_numeric_and_metadata`
- grain: `event-level measurement`

## Standard Definition

Hourly child bridge that preserves the approved ICU intake-output-balance backbone while adding conservative RRT extracorporeal fluid-removal evidence side by side. Opening v1 augments hourly output and net balance with default bridge evidence only, while retaining ultrafiltrate and peritoneal-dialysis rows as context-only columns.

## Default Presentation

- default display rule: `not yet separately codified`
- default anchor type: `ICU admission anchor`
- current implementation note: Hourly child bridge that augments the approved ICU intake-output balance backbone with conservative RRT fluid-removal evidence.

## Global Warnings

- `high` / `std_icu_rrt_fluid_removal_bridge_hourly caution 1`: this is a conservative bridge child, not a claim of complete whole-body fluid balance or complete extracorporeal-removal truth
- `medium` / `std_icu_rrt_fluid_removal_bridge_hourly caution 2`: the approved base hourly balance columns remain visible as the non-bridge baseline; use `bridge_augmented_*` fields only when you explicitly want RRT removal added
- `medium` / `std_icu_rrt_fluid_removal_bridge_hourly caution 3`: intermittent hemodialysis removal is often charted as a single therapy-end point total, so the hourly bridge can legitimately show pulse-like negative spikes rather than a smooth removal trajectory.
- `medium` / `std_icu_rrt_fluid_removal_bridge_hourly caution 4`: because bridge rows are aligned by recorded `charttime`, retrospective CRRT or IHD removal charting can differ by up to about one hour from forward-apportioned intake intervals; this phase difference is an opening-v1 timing contract, not a bug unique to this child.
- `medium` / `std_icu_rrt_fluid_removal_bridge_hourly caution 6`: `bridge_augmented_*net_balance*` adds RRT-side removal only; if circuit-related intake such as rinse-back or anticoagulation-associated fluids is not present in the carried-through intake numerators, augmented net balance can be mildly biased toward negative balance.
- `medium` / `std_icu_rrt_fluid_removal_bridge_hourly caution 7`: context-only PD and ultrafiltrate fields are retained for sensitivity and modality-specific work and must not be silently promoted into the opening main numerator
- `medium` / `std_icu_rrt_fluid_removal_bridge_hourly caution 8`: same-time default-candidate dedup is deterministic and conservative, but it can still hide semantically redundant charting at the exact same timestamp by design

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-15 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
