# Public Variable Card: std_gcs

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-04-22 09:56 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_gcs`
- standardized Chinese name: `格拉斯哥昏迷评分（GCS）事件`
- standardized English name: `Glasgow Coma Scale event`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `scores`
- standard unit: `score_points`
- value type: `numeric score`
- grain: `event-level measurement`

## Standard Definition

Event-level standardized Glasgow Coma Scale asset for MIMIC retained under the scores semantic family, using the official source-supplied derived gcs table and preserving total score, component context, ICU-relative time, official-rule audit flags, outlier judgment, and cleaned total-score output.

## Default Presentation

- default display rule: `integer`
- primary retained value column: `std_gcs_total_cleaned`
- default anchor type: `ICU admission anchor`
- current approved value range note: `[3, 15] score_points`

## Global Warnings

- `high` / `Total GCS may not equal the visible component sum`: In the official derived source, the retained total GCS can differ from the visible component sum because of ETT special handling or other official derivation behavior. Preferred handling: Interpret total GCS as the official derived target value, and treat component columns as contextual fields rather than a guaranteed re-summation basis.
- `medium` / `first_day_gcs is intentionally out of scope in this asset`: The first retained std_gcs asset is an event-level history and does not merge the first_day_gcs baseline concept. Preferred handling: If first-day or baseline GCS is needed later, retain it as a separate approved downstream asset.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-03-23 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
