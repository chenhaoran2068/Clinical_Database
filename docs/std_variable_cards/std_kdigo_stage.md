# Public Variable Card: std_kdigo_stage

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_kdigo_stage`
- standardized Chinese name: `KDIGO急性肾损伤分期时间序列`
- standardized English name: `KDIGO AKI stage time series`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `diagnosis_computable`
- standard unit: none
- value type: `mixed_numeric_or_text`
- grain: `icu_stay_event`

## Standard Definition

ICU-stay-linked standardized KDIGO AKI staging time-series asset for MIMIC retained under diagnosis_computable, using the official source-supplied derived kdigo_stages table and preserving the raw per-charttime KDIGO stage as the primary retained stage, while keeping the official 6-hour smoothed helper stage and same-row creatinine, urine-output, and CRRT evidence context.

## Default Presentation

- default display rule: `integer`
- primary retained value column: `std_kdigo_stage`
- default anchor type: `ICU admission anchor`
- current approved value range note: `0; 1; 2; 3`
- current implementation note: Official KDIGO AKI event-level staging with raw stage primary and 6h smoothed helper retained as explicit context.

## Global Warnings

- `high` / `Official KDIGO charttime is not limited to ICU-only clock time`: Some rows predate ICU intime because official KDIGO creatinine staging uses a 7-day pre-ICU lookback within the indexed stay; a very small number also appear after ICU outtime. Preferred handling: Use `time_scope_class` explicitly when restricting to within-ICU analyses.
- `high` / `Smoothed stage is helper context, not the default onset anchor`: The official source publishes a 6-hour smoothed stage intended to reduce oscillation, but it is semantically different from the raw stage at the current row. Preferred handling: Use `std_kdigo_stage` for primary event-stage interpretation unless a later contract explicitly chooses otherwise.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-08 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
