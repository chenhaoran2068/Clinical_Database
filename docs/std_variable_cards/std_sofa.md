# Public Variable Card: std_sofa

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_sofa`
- standardized Chinese name: `序贯器官衰竭评估（SOFA）`
- standardized English name: `Sequential Organ Failure Assessment`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `scores`
- standard unit: `score_points`
- value type: `numeric_score_time_series`
- grain: `icu_stay_hour`

## Standard Definition

Hourly ICU-stay standardized SOFA time-series asset for MIMIC retained under the scores semantic family, using the official source-supplied derived sofa table and preserving the rolling 24-hour total SOFA score as the primary standardized value, together with official same-row hourly organ component scores and rolling 24-hour organ component scores.

## Default Presentation

- default display rule: `integer`
- primary retained value column: `std_sofa_total_24hours`
- default anchor type: `source_sofa_hour_index`
- current approved value range note: `[0,24] score_points`
- current implementation note: Primary retained SOFA value is the official rolling 24-hour total at each ICU hour; current-hour and rolling organ component scores are kept as same-row official context.

## Global Warnings

- `high` / `SOFA source hour grid is not the same as exact ICU intime/outtime boundaries`: The official SOFA source inherits clock-hour rows from icustay_hourly, which is derived from first and last heart-rate measurement coverage rather than exact ICU stay boundaries. Preferred handling: Treat `score_hour_index` and the official source hour grid as the truthful retained time basis for this asset.
- `high` / `Early SOFA rows do not yet have a full 24-hour window`: The official source calculates SOFA every ICU hour, but rows before the end of the first 24 hours use a shorter effective data window. Preferred handling: Check `score_window_hours` and consider restricting to `score_window_hours == 24` when a full rolling window is required.
- `high` / `Current-hour and rolling SOFA components are not the same concept`: The hourly component columns reflect the current one-hour slice, while the *_24hours columns reflect rolling worst-component values across the effective window. Preferred handling: Do not mix hourly component values with rolling 24-hour component values as if they were interchangeable.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-01 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
