# Public Variable Card: std_crrt_device_parameter_event

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_crrt_device_parameter_event`
- standardized Chinese name: `CRRT设备参数事件`
- standardized English name: `CRRT device parameter event`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `device_support`
- standard unit: none
- value type: `mixed_numeric_text_event`
- grain: `icu_stay_event`

## Standard Definition

ICU-stay-level device-support event asset retaining one row per charted CRRT device-parameter or CRRT fluid-context event that overlaps an approved std_crrt_family_active episode, while preserving source-faithful charttime/storetime, raw values, standardized parameter naming, and explicit default versus specialized analysis eligibility.

## Default Presentation

- default display rule: `not yet separately codified`
- primary retained value column: `std_crrt_device_parameter_event`
- default anchor type: `ICU admission anchor`
- current implementation note: Use this retained CRRT device-parameter long table for source-faithful machine-setting and circuit-context analyses after explicit filtering by parameter_domain_class and the eligibility flags; this asset is not a CRRT active-state truth line and does not by itself resolve delivered dose, systemic anticoagulation, or whole-body fluid balance.

## Global Warnings

- `high` / `Not a support-state truth line`: Use std_crrt_family_active rather than this asset for support duration, support-day, and free-day style analyses.
- `high` / `Machine-side hourly removal record is not whole-body fluid balance`: hourly_patient_fluid_removal_volume must not be used in isolation as patient-level cumulative fluid balance; systemic inputs and outputs must be joined separately.
- `medium` / `Ultrafiltrate output is not guaranteed total-effluent truth`: ultrafiltrate_output_volume is retained as specialized context and should not be blindly treated as delivered dose or total-effluent truth.
- `medium` / `Citrate is physical circuit flow only`: citrate_acd_a_rate is retained because it is part of circuit flow context; this asset does not promise systemic anticoagulation or calcium-balance interpretation.
- `medium` / `Deferred child lines remain necessary`: System integrity, heparin context, and filter-change reason remain deferred to future child lines rather than being collapsed into the opening default parameter main path.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-13 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
