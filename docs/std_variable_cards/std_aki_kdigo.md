# Public Variable Card: std_aki_kdigo

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_aki_kdigo`
- standardized Chinese name: `KDIGO急性肾损伤（AKI）住院汇总`
- standardized English name: `AKI by KDIGO stay summary`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `diagnosis_computable`
- standard unit: none
- value type: `mixed_numeric_or_text`
- grain: `ICU stay`

## Standard Definition

Standardized AKI by KDIGO stay summary. Value type: mixed_numeric_or_text. Grain: ICU stay.

## Default Presentation

- default display rule: `not yet separately codified`
- primary retained value column: `std_aki_kdigo`
- default anchor type: `ICU admission anchor`
- current approved value range note: `false; true`
- current implementation note: Stay-level AKI summary from official KDIGO stage events with raw-positive primary flag and smoothed helper context retained.

## Global Warnings

- `high` / `Primary flag and smoothed helper are intentionally different`: This summary keeps both the raw-based primary AKI flag and a smoothed sensitivity helper because the official source publishes both concepts. Preferred handling: Default to `std_aki_kdigo` for primary diagnosis reporting and mention `aki_flag_kdigo_smoothed_6hr` only as sensitivity context.
- `high` / `Some AKI onset times predate ICU admission`: The earliest raw-positive KDIGO row can occur before ICU intime because pre-ICU creatinine evidence is allowed in the official source within the indexed stay. Preferred handling: Use `aki_first_positive_phase` to separate pre-ICU-present AKI from ICU-after-intime onset.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-08 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
