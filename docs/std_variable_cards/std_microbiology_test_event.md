# Public Variable Card: std_microbiology_test_event

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-04-22 09:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_microbiology_test_event`
- standardized Chinese name: `微生物检验事件`
- standardized English name: `Microbiology Test Event`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `laboratory`
- standard unit: none
- value type: `categorical text`
- grain: `1 row per micro_specimen_id + test_seq`

## Standard Definition

Standardized Microbiology Test Event. Value type: categorical text. Grain: 1 row per micro_specimen_id + test_seq.

## Default Presentation

- default display rule: `not yet separately codified`
- default anchor type: `hospital_admission_if_available_else_subject_only`

## Global Warnings

- `high` / `negative_inference`: Do not treat `has_organism_child = False` alone as definitive negative culture or stain. Preferred handling: Prefer `event_positivity_analysis_class` and review `event_comment_text` together.
- `high` / `conservative_regex_layer`: Result classes are additive conservative regex-derived helpers, not replacements for raw microbiology text. Preferred handling: Keep `event_comment_text` visible and use `event_result_inference_basis` to understand how the class was assigned.
- `high` / `comment_vs_isolate_conflict`: Some events have positive isolate-like organism branches while comment text suggests negative, mixed, commensal, quantity-only, or cancelled semantics. Preferred handling: Manually review these rows before treating them as simple positives or negatives.
- `high` / `orders_vs_results`: Opening microbiology assets retain performed laboratory results only, not order-entry artifacts. Preferred handling: Keep orders-side work deferred to the future orders family.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-02 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
