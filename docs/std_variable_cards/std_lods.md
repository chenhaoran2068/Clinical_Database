# Public Variable Card: std_lods

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-04-22 09:57 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_lods`
- standardized Chinese name: `逻辑性器官功能障碍评分（LODS）`
- standardized English name: `Logistic Organ Dysfunction System`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `scores`
- standard unit: `score_points`
- value type: `numeric score`
- grain: `ICU stay`

## Standard Definition

Stay-level standardized LODS summary asset for MIMIC retained under the scores semantic family, using the official source-supplied derived lods table and preserving the primary total score, first-day ICU score window, and official same-row organ-system component subscores.

## Default Presentation

- default display rule: `integer`
- primary retained value column: `std_lods_total`
- default anchor type: `icu_first_day_window`

## Global Warnings

- `high` / `First-day score window is not an exact event timestamp`: These assets summarize the first ICU-day scoring window rather than a single instantaneous measurement. Preferred handling: Use `score_window_start` and `score_window_end` as the truthful time bounds.
- `medium` / `Official context columns are not separate standardized assets in this opening round`: Subscores and probability columns are retained on the same row as official context for the main score asset. Preferred handling: Treat the main score column as primary and the other retained score columns as same-row official context unless a later asset explicitly splits them.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-01 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
