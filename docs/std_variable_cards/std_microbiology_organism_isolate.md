# Public Variable Card: std_microbiology_organism_isolate

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_microbiology_organism_isolate`
- standardized Chinese name: `微生物分离株`
- standardized English name: `Microbiology Organism Isolate`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `laboratory`
- standard unit: none
- value type: `categorical text`
- grain: `1 row per micro_specimen_id + test_seq + organism branch`

## Standard Definition

Standardized Microbiology Organism Isolate. Value type: categorical text. Grain: 1 row per micro_specimen_id + test_seq + organism branch.

## Default Presentation

- default display rule: `not yet separately codified`
- default anchor type: `hospital_admission_if_available_else_subject_only`
- current implementation note: Organism branch asset under the microbiology event hierarchy. The organism branch is retained separately so culture growth and isolate identity are not flattened into the parent event or the antibiotic-susceptibility leaf rows.

## Global Warnings

- `high` / `quantity_text`: Quantity text is left raw and may be semi-structured. Preferred handling: Keep it as descriptive microbiology text unless a later approved parser is built.
- `high` / `branch_role_semantics`: Not every organism branch is a true isolate-like branch; some are cancelled placeholders or flora-summary labels. Preferred handling: Use `organism_positive_analysis_class` for direct positive-isolate filtering.
- `medium` / `organism_ontology`: This opening build preserves raw organism labels without external ontology harmonization. Preferred handling: Use raw labels now and add ontology harmonization only after explicit review.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-02 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
