# Public Variable Card: std_sofa2

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_sofa2`
- standardized Chinese name: `SOFA-2评分`
- standardized English name: `SOFA-2`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `scores`
- standard unit: none
- value type: `integer_score_with_component_trace`
- grain: `event-level measurement`

## Standard Definition

Hourly ICU SOFA-2 retained main score stream built on the reviewed std_sofa backbone, reusing approved helper-derived cardiovascular, brain, and kidney components, directly scoring respiratory/liver/hemostasis from retained evidence, and carrying rolling 24-hour organ maxima plus rolling 24-hour total score.

## Default Presentation

- default display rule: `integer`
- primary retained value column: `std_sofa2_total_24hours`
- default anchor type: `ICU admission anchor`
- current implementation note: Opening v1 uses PF primary, SF fallback from SpO2 plus latest-prior FiO2, advanced-support gating for respiratory score 3-4, any-ECMO respiratory override to 4, and explicit approximation flags instead of hidden bedside assumptions.

## Global Warnings

- `high` / `respiratory_exception_boundary`: Published SOFA-2 allows rare respiratory score >2 without advanced support when support is unavailable or ceiling-of-care limits its use, but opening v1 does not directly reconstruct that exception. Preferred handling: Treat the opening retained line as the conservative default and build a dedicated sensitivity layer if that distinction matters.
- `medium` / `home_ventilation_boundary`: Published SOFA-2 examples include long-term home ventilation in the advanced-support concept, but current retained MIMIC foundations do not directly reconstruct that chronic support context. Preferred handling: Do not silently assume home-ventilation support from other chronic-respiratory fields; build a dedicated boundary layer instead.
- `medium` / `stay_link_boundary`: Direct respiratory/liver/hemostasis inputs are projected only from retained stay-linked rows; hospital-only rows without stay linkage are not re-matched from raw sources inside this build. Preferred handling: Build a separately reviewed relinking layer rather than silently broadening this retained score path.
- `medium` / `helper_contract_dependency`: Cardiovascular, brain, and kidney components inherit reviewed helper semantics rather than recomputing those systems again inside std_sofa2. Preferred handling: Audit or revise the helper contracts first, then rebuild std_sofa2 from the approved helper outputs.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-11 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
