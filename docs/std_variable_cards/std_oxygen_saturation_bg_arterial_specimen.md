# Public Variable Card: std_oxygen_saturation_bg_arterial_specimen

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
Owner approval closure recorded on 2026-05-29.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_oxygen_saturation_bg_arterial_specimen`
- standardized Chinese name: `血气氧饱和度事件（动脉标本）`
- standardized English name: `Blood-gas oxygen saturation event (arterial specimen)`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `laboratory`
- standard unit: `percent`
- value type: `numeric measurement`
- grain: `event-level measurement`

## Standard Definition

Standardized Blood-gas oxygen saturation event (arterial specimen). Value type: numeric measurement. Grain: event-level measurement. Standard unit: percent.

## Default Presentation

- default display rule: `not yet separately codified`
- default anchor type: `icu_intime_if_uniquely_linked`
- current approved value range note: `[1, 100] percent`

## Global Warnings

- `high` / `This is an event-level laboratory asset`: The retained Layer 3 output keeps one row per retained laboratory event, not one row per patient, admission, or ICU stay. Preferred handling: If baseline laboratory values are needed later, derive a separate approved baseline asset from this event asset.
- `medium` / `All numeric rows are retained`: Current retained scope keeps all numeric raw rows, including 0 and negative values when present. Preferred handling: Review the variable contract before interpreting 0 or negative values as automatically invalid.
- `high` / `legacy_companion_scope`: This long-name asset is a MIMIC legacy strict-arterial companion, not the cross-database canonical SaO2 trunk. Cross-database SaO2 analyses should use canonical `std_sao2` and strict arterial eligibility fields.
- `high` / `not_spo2`: Do not fill this invasive arterial blood-gas table with bedside pulse oximetry SpO2.
- `high` / `amsterdam_blocked_candidate`: Amsterdam has a materialized but blocked candidate that mostly mirrors all-specimen SO2 and only excludes rows explicitly commented as venous. `comment NOT LIKE "%venous%"` is not arterial confirmation.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- AmsterdamUMCdb-1.0.2 is reviewed-approved unavailable as a materialized blocked candidate because structured arterial specimen confirmation is unavailable.
- eICU-CRD-2.0, SICdb-1.0.8, and NWICU-0.1.0 are reviewed-approved unavailable for this legacy companion; use canonical `std_sao2` instead.
- current publication basis: owner-approved 2026-05-29 local Layer 5 review closure

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-05-29 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
