# Public Variable Card: std_septic_shock_onset

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_septic_shock_onset`
- standardized Chinese name: `脓毒性休克起病`
- standardized English name: `Septic shock onset`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `diagnosis_computable`
- standard unit: none
- value type: `mixed_numeric_or_text`
- grain: `ICU stay`

## Standard Definition

ICU-stay-level septic-shock onset summary using approved M3 as the primary retained method and M3-strict as a parallel strict sensitivity line.

## Default Presentation

- default display rule: `not yet separately codified`
- primary retained value column: `suspected_infection_time`
- default anchor type: `ICU admission anchor`
- current implementation note: One row per ICU stay, with primary M3 septic shock summary plus strict sensitivity companion fields.

## Global Warnings

- `high` / `Primary septic-shock flag uses the opening M3 method`: The default summary flag is intentionally the approved primary opening method rather than the stricter sensitivity line. Preferred handling: Use the strict companion columns explicitly instead of assuming the primary flag is the strict line.
- `high` / `Fluid adequacy remains deferred`: Opening v1 does not force a hard fluid-resuscitation gate. Preferred handling: Treat this summary as the approved retrospective opening operationalization.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-08 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
