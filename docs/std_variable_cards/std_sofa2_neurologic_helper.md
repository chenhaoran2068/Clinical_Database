# Public Variable Card: std_sofa2_neurologic_helper

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_sofa2_neurologic_helper`
- standardized Chinese name: `SOFA-2神经系统辅助表`
- standardized English name: `SOFA-2 neurologic helper`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `scores`
- standard unit: none
- value type: `integer_score_with_rule_trace`
- grain: `event-level measurement`

## Standard Definition

Hourly ICU SOFA-2 neurologic helper line built on the retained std_sofa backbone, with latest-prior GCS projection, explicit motor-only fallback, approved default delirium-treatment minimum-score logic, and explicit notice that exact pre-sedation GCS is not directly reconstructed in opening v1.

## Default Presentation

- default display rule: `string`
- primary retained value column: `score_time_precision`
- default anchor type: `ICU admission anchor`
- current implementation note: Opening v1 keeps pre-sedation GCS unresolved, uses explicit motor-only fallback crosswalk, and applies delirium-treatment only as a minimum-1 raise when the exam-based candidate is 0 or null.

## Global Warnings

- `high` / `pre_sedation_boundary`: The source paper says to use the last pre-sedation GCS, but current retained MIMIC foundations do not directly reconstruct that bedside boundary. Preferred handling: Treat opening helper as the auditable repository default and build a dedicated sedation-timeline sensitivity layer if that distinction matters.
- `medium` / `motor_only_crosswalk`: Motor-only fallback uses an explicit repository crosswalk when total GCS is unavailable. Preferred handling: Treat the opening crosswalk as the default auditable path and build a separate sensitivity layer rather than silently changing helper semantics.
- `medium` / `delirium_treatment_semantics`: The delirium-related branch uses approved treatment evidence, not structured delirium diagnosis truth. Preferred handling: Keep this helper aligned to the approved SOFA-2 treatment-based rule surface and build separate delirium-diagnosis analyses outside the helper.
- `medium` / `same_time_assessment_boundary`: The retained same-time CAM-ICU summary exists but is intentionally not used as a direct scoring input in opening v1. Preferred handling: Use the same-time assessment asset as QC/context only unless a new reviewed contract explicitly changes the neurologic score path.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-11 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
