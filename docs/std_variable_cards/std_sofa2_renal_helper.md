# Public Variable Card: std_sofa2_renal_helper

Generated by `scripts/layer5/export_public_variable_card.py` on 2026-05-07 22:58 UTC.
This is a GitHub-safe public metadata summary, not the full local Layer 5 execution evidence package.

## Identity

- `std_variable_id`: `std_sofa2_renal_helper`
- standardized Chinese name: `SOFA-2肾脏辅助表`
- standardized English name: `SOFA-2 renal helper`
- standardized Japanese name: not yet approved for public publication
- semantic folder: `scores`
- standard unit: none
- value type: `integer_score_with_rule_trace`
- grain: `event-level measurement`

## Standard Definition

Hourly ICU SOFA-2 renal helper line built on the retained std_sofa backbone, with latest-prior KDIGO context projection, parent-RRT stage-4 carryover, conservative exact-paired acid-base stage-4 logic, and explicit approximation notes for anuria proxy and unresolved RRT indication.

## Default Presentation

- default display rule: `string`
- primary retained value column: `score_time_precision`
- default anchor type: `ICU admission anchor`
- current implementation note: Opening v1 uses urine-output-rate context already retained inside std_kdigo_stage, applies the anuria clause through zero 12-hour rate proxy, and does not directly reconstruct non-renal-purpose RRT exclusion.

## Global Warnings

- `high` / `rrt_indication_boundary`: The source paper excludes RRT used only for non-renal purposes, but current retained MIMIC truth does not directly reconstruct RRT indication. Preferred handling: Treat opening helper stage-4 RRT rows as source-faithful approximations and perform dedicated sensitivity analyses if that distinction matters.
- `medium` / `exact_pair_requirement`: The non-RRT biochemical stage-4 acidemia trigger uses only exact same-time pH+bicarbonate pairs. Preferred handling: Treat opening helper as the strict auditable path and rebuild a separate sensitivity layer if looser matching is desired.
- `medium` / `stage0_boundary`: Opening v1 gives stage 0 only when effective creatinine <= 1.2; normal urine-output evidence alone does not create stage 0. Preferred handling: Read score_rule_branch_class and creatinine_stage_hourly_candidate before collapsing null rows into stage 0.

## Cross-Database Status

- Currently approved in MIMIC-IV-3.1 only.
- current publication basis: reviewed-approved `MIMIC-IV-3.1` knowledge package

## Current Approved Database Assets

| database_id | current_status | latest_version | latest_review_date |
| --- | --- | --- | --- |
| MIMIC-IV-3.1 | reviewed_approved | v1 | 2026-04-11 |

## Publication Rule

Detailed source-table mappings, database-specific build logs, grouped review history, and rerun assessment remain in local Layer 5 evidence packages rather than this public card.
The public-card exporter applies a conservative publication filter and suppresses local-only implementation detail such as raw source fields, itemids, and local threshold mechanics.
